<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <style>
        body {
            background-color: #141d26; /* Twitter's dark background color */
            color: #ffffff; /* Text color */
            font-family: cursive;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        #container {
            width: 100%;
            text-align: center;
        }

        h1 {
            color: #1da1f2; /* Twitter's blue color */
        }

        form {
            width: 300px;
            margin: 0 auto; /* Center the form */
            text-align: center;
        }

        label {
            display: block;
            margin-top: 10px;
            color: #ffffff;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-top: 6px;
            margin-bottom: 12px;
            box-sizing: border-box;
            background-color: #192734; /* Twitter's input background color */
            color: #ffffff;
            border: 1px solid #38444d; /* Twitter's input border color */
            border-radius: 4px;
        }

        button {
            background-color: #1da1f2; /* Twitter's blue color */
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0d8bf0; /* Darker shade on hover */
        }

        p {
            color: red;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div id="container">
        <h1>Change Password</h1>
        <% if (error) { %>
            <p><%= error %></p>
        <% } %>
        <form action="/changepassword_submit" method="post">
            <label for="currentPassword">Current Password:</label>
            <input type="password" id="currentPassword" name="currentPassword" required>

            <label for="newPassword">New Password:</label>
            <input type="password" id="newPassword" name="newPassword" required>

            <label for="confirmPassword">Confirm Password:</label>
            <input type="password" id="confirmPassword" name="confirmPassword" required>

            <button type="submit">Change Password</button>
        </form>
    </div>
</body>
</html>
